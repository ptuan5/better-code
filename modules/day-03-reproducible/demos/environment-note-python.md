# Model Count Summary

## Requirements

- Python 3
- `pandas`

## Run

Run `python3 example.py` from `activities/activity-1/`.

## Input

The script uses the built-in `gene_set_uploads` table defined in the file.

## Output

The script writes `results/model_counts.csv`.
